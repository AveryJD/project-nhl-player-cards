[build-system]
requires = ["setuptools>=65.5.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nhl-player-stat-cards"
version = "0.1.0"
description = "NHL Player Stat Card generation project"
authors = [{ name = "Avery Doiron" }]
requires-python = ">=3.9"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "CairoSVG>=2.8.2",
    "joblib>=1.5.2",
    "matplotlib>=3.10.7",
    "numpy>=2.3.5",
    "pandas>=2.3.3",
    "pillow>=12.0.0",
    "requests>=2.32.5",
    "scikit-learn>=1.7.2",
    "scipy>=1.16.3",
    "threadpoolctl>=3.6.0",
]

[tool.setuptools.packages.find]
where = ["src"]